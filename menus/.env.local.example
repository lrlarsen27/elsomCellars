# Copy this file to `.env.local` and fill in real values.
# `.env.local` is gitignored — never commit it.

# Nothing is required here yet. The spreadsheet and tab ids land in this file
# when the content loader arrives; they are public by construction, since they
# ship inside the page's JavaScript.
