# Repo-wide ignores. These patterns are unanchored, so they apply to every
# project directory in this repo — including ones added later.

node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Local secrets — never commit
.env.local
.env*.local

# OS cruft
.DS_Store
Thumbs.db

# --- menus ---
# Local menu data store. Hosted deployments use a real database instead.
menus/data/
